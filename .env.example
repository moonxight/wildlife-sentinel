# Enter the real URL in Render's Environment dashboard, never in Git.
# Empty DATABASE_URL keeps the existing XAMPP/MySQL connection.
DATABASE_URL=
# Example format: postgresql://USER:PASSWORD@HOST/neondb?sslmode=require&channel_binding=require
# This app reads process environment variables; it does not load this file.
# No separate DB_DRIVER variable is required.
